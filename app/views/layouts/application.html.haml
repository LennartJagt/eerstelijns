!!! 
%html
  %head
    %title= h(yield(:title) || "Untitled")
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = yield(:head)
  %body
    #container
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"
      = link_to "Alle praktijken", practices_path
      = link_to "Nieuwe Praktijk", new_practice_path
      = link_to "Nieuwe bezoeken toevoegen", new_visit_path
      - if show_title?
        %h1= h yield(:title)
        
      = yield